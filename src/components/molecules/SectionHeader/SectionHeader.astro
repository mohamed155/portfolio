---
interface Props {
  index: string;
  title: string;
  meta?: string;
  strong?: boolean;
  metaAccent?: boolean;
  class?: string;
}

const { index, title, meta, strong = false, metaAccent = false, class: className } = Astro.props;
---

<div
  class:list={[
    'flex items-baseline gap-[18px] border-b pb-[14px]',
    strong ? 'border-b-bd2' : 'border-b-bd',
    className,
  ]}
>
  <span class="text-[11px] tracking-caps text-ac">{index}</span>
  <h2 class="font-display text-body-xs font-bold uppercase tracking-caps-widest text-fg">{title}</h2>
  {meta && (
    <span class:list={['ml-auto text-label-sm tracking-caps', metaAccent ? 'text-ac' : 'text-fg3']}>{meta}</span>
  )}
</div>
